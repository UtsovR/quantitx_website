# Quantitx

Premium fintech marketing site built with React, Vite, TypeScript, and CSS.

## Commands

- `npm run dev` — start local development
- `npm run build` — type-check and create the Netlify-ready production build in `dist`
- `npm run preview` — preview the production build locally

Supabase remains intentionally unimplemented until backend work is approved.
